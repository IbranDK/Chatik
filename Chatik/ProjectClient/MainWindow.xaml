<Window x:Class="ProjectClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Чат" Height="500" Width="700">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="160"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Панель подключения -->
        <StackPanel Grid.Row="0" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="5">
            <TextBlock Text="IP:" VerticalAlignment="Center"/>
            <TextBox x:Name="TxtHost" Width="120" Margin="5,0" Text="127.0.0.1"/>
            <TextBlock Text="Порт:" VerticalAlignment="Center"/>
            <TextBox x:Name="TxtPort" Width="50" Margin="5,0" Text="5000"/>
            <TextBlock Text="Ник:" VerticalAlignment="Center"/>
            <TextBox x:Name="TxtNick" Width="100" Margin="5,0"/>
            <Button Content="Подключиться" Width="110" Click="BtnConnect_Click"/>
        </StackPanel>

        <!-- Лента сообщений -->
        <ListBox x:Name="MessageList" Grid.Row="1" Grid.Column="0" Margin="5"/>

        <!-- Список пользователей -->
        <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,5,5,5">
            <TextBlock Text="Онлайн:" FontWeight="Bold" Margin="0,0,0,5"/>
            <ListBox x:Name="UserList"/>
        </StackPanel>

        <!-- Поле ввода -->
        <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="5">
            <TextBox x:Name="TxtInput" Width="500" Margin="0,0,10,0"
                     KeyDown="TxtInput_KeyDown"/>
            <Button Content="Отправить" Width="100" Click="BtnSend_Click"/>
        </StackPanel>
    </Grid>
</Window>